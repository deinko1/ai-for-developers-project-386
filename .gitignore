# macOS
.DS_Store

# Local environment files (keep the examples committed)
.env
.env.*
!.env.example
*.local

# mise local overrides (machine-specific)
.mise.local.toml

# Node tooling at the repo root (commitlint)
node_modules

# Logs
*.log
