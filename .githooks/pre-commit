#!/bin/sh
# Runs linters, tests and type checks before each commit.
# Bypass with: git commit --no-verify
set -e

cd "$(git rev-parse --show-toplevel)"

echo "pre-commit: make check (lint + tests + typecheck)"
make check
